<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
  </PropertyGroup>

  <ItemGroup>
    <!-- No Cosmos DB SDK here: this project ships inside the importer, web site and installer
         packages. The Cosmos-backed stores live in src/TelemetryService, their only consumer. -->
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" Version="10.0.11" />
    <PackageReference Include="Newtonsoft.Json" Version="13.0.4" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\DataUtils\DataUtils.csproj" />
  </ItemGroup>

</Project>
