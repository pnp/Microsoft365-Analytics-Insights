legacy-peer-deps=false

# Fail fast when the running Node major does not match the `engines` range in package.json.
# Without this npm only prints a warning, so a machine on the wrong major still installs and
# builds - which is exactly the inconsistency the repo-root .nvmrc pin exists to prevent.
engine-strict=true